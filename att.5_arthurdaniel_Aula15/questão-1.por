programa {
    funcao inicio() {
    // (a) SE aninhado
    // Porque é necessário analisar diferentes valores do IMC.

    // (b) ESCOLHA/CASO
    // Porque cada tecla possui uma opção propria.

    // (c) SE aninhado
    // Porque é necessário verificar diferentes faixas de distância.

    // (d) ESCOLHA/CASO
    // Porque cada número do turno corresponde a uma opção.
    }
}
